> **Note:** this folder is published to GitHub Pages by
> `.github/workflows/pages.yml` (Pages → Source: *GitHub Actions*).

# Online dashboard (GitHub Pages)

AUTO-GENERATED from `smart-dehumidifier/src/webui.h` by
`smart-dehumidifier/tools/online/sync-online.js` — edit `webui.h`, re-run the
script, commit both.

Open it while connected to the **AgarbattiDryer** hotspot via
**http://192.168.4.1/online** (the ESP bridges API calls, because browsers
block `https → http` requests). Opened directly, the page runs standalone
with limited function and explains what to do.